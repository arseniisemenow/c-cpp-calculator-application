#ifndef STRING_VALIDATOR_H_
#define STRING_VALIDATOR_H_

#include <QMainWindow>
#include <QRegularExpression>

bool isDoubleNumber(const QString &input);

bool isIntNumber(const QString &input);

#endif // STRING_VALIDATOR_H_
